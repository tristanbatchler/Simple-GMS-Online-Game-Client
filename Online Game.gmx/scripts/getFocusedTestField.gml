///getFocusedTestField() -> returns the instance id of the text field that is focused (there should only be one!!)

with(parTextField){
    if(focus == true){
        return id;
        break;
    }
}

return noone;
